node_modules/
build/
# dist/ (tracked for direct Hostinger / cPanel deployment without requiring build tools on server)
coverage/
.DS_Store
*.log
.env*
!.env.example

# Exclude giant backup directories to prevent deployment zip corruption
Before_*/
Final_*/
backups/
wa_auth/
uploads/
*.txt
*.rules
*.blueprint.json

